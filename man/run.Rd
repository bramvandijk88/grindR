% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grind.R
\name{run}
\alias{run}
\title{Numerically integrate an ODE model over time}
\usage{
run(
  tmax = 100,
  tstep = 1,
  state = s,
  parms = p,
  odes = model,
  ymin = 0,
  ymax = NULL,
  log = "",
  xlab = "Time",
  ylab = "Density",
  tmin = 0,
  draw = lines,
  times = NULL,
  show = NULL,
  arrest = NULL,
  events = NULL,
  after = NULL,
  tweak = NULL,
  timeplot = TRUE,
  traject = FALSE,
  table = FALSE,
  add = FALSE,
  legend = TRUE,
  solution = FALSE,
  delay = FALSE,
  lwd = 2,
  col = "black",
  pch = 20,
  ...
)
}
\arguments{
\item{tmax}{Final time of the integration.}

\item{tstep}{Time increment between successive output points.}

\item{state}{Named numeric vector of initial values for the state
variables. Defaults to the global \code{s}.}

\item{parms}{Named numeric vector of parameters. Defaults to the global
\code{p}.}

\item{odes}{The model: a function \code{f(t, state, parms)} returning the
derivatives as \code{list(c(...))}. Defaults to the global \code{model}.}

\item{ymin, ymax}{Lower and upper limits of the y-axis (\code{ymax = NULL}
auto-scales).}

\item{log}{Which axes to draw on a log scale: \code{""}, \code{"x"},
\code{"y"} or \code{"xy"}.}

\item{xlab, ylab}{Axis labels for the time plot.}

\item{tmin}{Start time of the integration.}

\item{draw}{Function used to draw the time course, typically
\code{\link[graphics]{lines}} or \code{\link[graphics]{points}}.}

\item{times}{Optional explicit vector of output times. If supplied it
overrides \code{tmin}, \code{tmax} and \code{tstep}.}

\item{show}{Names or indices of the state variables to plot (default: all).}

\item{arrest}{Times, or names of parameters holding times, at which the
integrator is forced to stop exactly (useful at discontinuities). Cannot
be combined with \code{events}.}

\item{events}{A \code{\link[deSolve]{events}} specification passed to the
solver for discrete changes to the state.}

\item{after}{A string of R code evaluated after each time step; may modify
\code{state} and \code{parms}. Cannot be combined with \code{solution} or
\code{delay}.}

\item{tweak}{A string of R code evaluated once after integration, operating
on the result data frame \code{nsol}.}

\item{timeplot}{If \code{TRUE}, draw the time course.}

\item{traject}{If \code{TRUE}, add the trajectory to the current phase plane
(see \code{\link{plane}}) instead of drawing a time plot.}

\item{table}{If \code{TRUE}, return the full time series as a data frame
instead of only the final state.}

\item{add}{If \code{TRUE}, add to the existing plot rather than starting a
new one.}

\item{legend}{If \code{TRUE}, draw a legend on the time plot.}

\item{solution}{If \code{TRUE}, treat \code{odes} as an explicit solution
\code{f(t)} evaluated directly, rather than integrating derivatives.}

\item{delay}{If \code{TRUE}, integrate delay differential equations with
\code{\link[deSolve]{dede}}.}

\item{lwd, col, pch}{Line width, colour and plotting character for the
trajectory / time plot.}

\item{...}{Additional arguments passed on to the solver
(e.g. \code{method}, \code{atol}, \code{rtol}) or to \code{plot}.}
}
\value{
By default, a named numeric vector with the state at \code{tmax}.
  If \code{table = TRUE}, a data frame with a \code{time} column followed by
  one column per state variable.
}
\description{
\code{run()} solves a system of ordinary differential equations (via
\code{\link[deSolve]{ode}}), optionally plots the time course, and returns
the final state. Because it returns the end point as a named vector, the
result can be fed back into \code{run()}, \code{\link{plane}} or
\code{\link{newton}}.
}
\examples{
model <- function(t, state, parms) {
  with(as.list(c(state, parms)), {
    dR <- b * R * (1 - R / K) - d * R
    list(c(dR))
  })
}
p <- c(b = 1, d = 0.1, K = 2)
s <- c(R = 0.1)

run(tmax = 50)                     # integrate and plot the time course
run(tmax = 50, timeplot = FALSE)   # just the final state
run(table = TRUE)                  # the full time series as a data frame

}
\seealso{
\code{\link{plane}}, \code{\link{newton}}, \code{\link{fit}}
}
