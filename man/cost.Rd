% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grind.R
\name{cost}
\alias{cost}
\title{Cost function minimised by fit()}
\usage{
cost(
  datas,
  odes,
  state,
  parms,
  guess,
  free,
  differ,
  fixed,
  fun,
  logpar,
  ParsFree,
  initial,
  isVar,
  ...
)
}
\arguments{
\item{datas}{List of observation data frames (as prepared by \code{fit}).}

\item{odes}{The model function \code{f(t, state, parms)}.}

\item{state}{Named numeric vector of initial state values.}

\item{parms}{Named numeric vector of parameters.}

\item{guess}{Named numeric vector of the parameter values currently being
tried (supplied by \code{\link[FME]{modFit}}).}

\item{free}{Names of the shared free parameters/states.}

\item{differ}{Names of the per-dataset parameters/states.}

\item{fixed}{Named list of per-dataset fixed values.}

\item{fun}{Optional function applied to the data and model output before the
cost is computed.}

\item{logpar}{If \code{TRUE}, \code{guess} is on a log scale.}

\item{ParsFree}{Names of the free parameters that are model parameters
rather than state variables (set internally by \code{fit}).}

\item{initial}{If \code{TRUE}, take each dataset's initial state from its
first row.}

\item{isVar}{Logical vector marking which names are state variables (set
internally by \code{fit}).}

\item{...}{Additional arguments passed on to \code{run} or
\code{\link[FME]{modCost}}.}
}
\value{
An \code{\link[FME]{modCost}} object giving the residuals and cost
  used by \code{\link[FME]{modFit}}.
}
\description{
\code{cost()} measures the mismatch between the model and the data for a
candidate parameter vector, returned as an \code{\link[FME]{modCost}} object.
It runs the model at the observation times and compares it with the data
across all datasets. This is the default \code{costfun} used by
\code{\link{fit}}; you rarely call it directly, but you can supply your own
via \code{fit(costfun = ...)}.
}
\seealso{
\code{\link{fit}}
}
