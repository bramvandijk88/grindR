% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grind.R
\name{plane}
\alias{plane}
\title{Draw a phase plane with nullclines, vector field and trajectories}
\usage{
plane(
  xmin = -0.001,
  xmax = 1.05,
  ymin = -0.001,
  ymax = 1.05,
  xlab = "",
  ylab = "",
  log = "",
  npixels = 500,
  state = s,
  parms = p,
  odes = model,
  x = 1,
  y = 2,
  time = 0,
  grid = 5,
  show = NULL,
  addone = FALSE,
  portrait = FALSE,
  vector = 0,
  add = FALSE,
  legend = TRUE,
  zero = TRUE,
  lwd = 2,
  col = "black",
  pch = 20,
  vectorlen = 1,
  arrowsize = 0.05,
  ...
)
}
\arguments{
\item{xmin, xmax}{Limits of the horizontal axis (the \code{x} variable).}

\item{ymin, ymax}{Limits of the vertical axis (the \code{y} variable).}

\item{xlab, ylab}{Axis labels; default to the state-variable names.}

\item{log}{Which axes to draw on a log scale: \code{""}, \code{"x"},
\code{"y"} or \code{"xy"}.}

\item{npixels}{Grid resolution used to compute the nullclines. Higher is
smoother but slower.}

\item{state}{Named numeric vector of state values. Non-axis variables are
held at these values (see \code{zero}). Defaults to the global \code{s}.}

\item{parms}{Named numeric vector of parameters. Defaults to the global
\code{p}.}

\item{odes}{The model function \code{f(t, state, parms)}. Defaults to the
global \code{model}.}

\item{x, y}{State variable (index or name) on the horizontal and vertical
axis. Default \code{1} and \code{2}.}

\item{time}{Time at which the derivatives are evaluated (for
non-autonomous models).}

\item{grid}{Number of points per axis for the vector field and phase
portrait.}

\item{show}{Names or indices of the variables whose nullclines are drawn
(default: the two axis variables).}

\item{addone}{If \code{TRUE}, plot \code{variable + 1} so a log axis can
include zero.}

\item{portrait}{If \code{TRUE}, integrate and draw trajectories from a grid
of starting points.}

\item{vector}{Vector field to overlay: \code{0} none (default), \code{1}
sign-only arrows (one per axis), \code{2} a single arrow in the flow
direction, \code{3} sign-only segments without arrowheads.}

\item{add}{If \code{TRUE}, draw onto the existing phase plane (reusing its
axes and limits) instead of starting a new one.}

\item{legend}{If \code{TRUE}, draw a legend of the variable colours.}

\item{zero}{If \code{TRUE}, set all non-axis state variables to zero before
computing the plane; if \code{FALSE}, hold them at their \code{state}
values.}

\item{lwd, col, pch}{Line width of the nullclines, colour of the phase-portrait
trajectories, and plotting character of their starting points.}

\item{vectorlen}{Scaling factor for the length of the vectors.}

\item{arrowsize}{Size of the arrowheads.}

\item{...}{Additional arguments passed on to \code{run} (for the phase
portrait) or to \code{plot}.}
}
\value{
None; \code{plane} is called for the plot it draws.
}
\description{
\code{plane()} plots a two-dimensional phase plane for two state variables.
It draws the nullcline of each variable (the curve where its derivative is
zero); intersections of nullclines are equilibria. Optionally it overlays a
vector field (\code{vector}) and/or a phase portrait of trajectories
(\code{portrait}). The chosen axes are remembered, so a subsequent
\code{run(traject = TRUE)} or \code{plane(add = TRUE)} draws onto the same
plane.
}
\examples{
model <- function(t, state, parms) {
  with(as.list(c(state, parms)), {
    dR <- b * R * (1 - R / K) - a * R * N / (R + h)
    dN <- c * a * R * N / (R + h) - delta * N
    list(c(dR, dN))
  })
}
p <- c(b = 1, K = 2, a = 1, c = 1, delta = 0.3, h = 1)
s <- c(R = 1, N = 1)

plane(xmax = 2, ymax = 2)               # nullclines only
plane(xmax = 2, ymax = 2, vector = 1)   # add sign-only arrows
run(traject = TRUE)                     # overlay a trajectory from s

}
\seealso{
\code{\link{run}}, \code{\link{newton}}, \code{\link{continue}}
}
