% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grind.R
\name{timePlot}
\alias{timePlot}
\title{Plot a time series}
\usage{
timePlot(
  data,
  tmin = 0,
  tmax = NULL,
  ymin = 0,
  ymax = NULL,
  log = "",
  xlab = "Time",
  ylab = "Density",
  show = NULL,
  legend = TRUE,
  draw = lines,
  lwd = 2,
  add = FALSE,
  main = NULL,
  sub = NULL,
  colMap = NULL,
  pchMap = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame whose first column is time and whose remaining
columns are the variables to plot.}

\item{tmin, tmax}{Time range (\code{tmax = NULL} uses the data range).}

\item{ymin, ymax}{Vertical range (\code{ymax = NULL} auto-scales).}

\item{log}{Which axes to draw on a log scale: \code{""}, \code{"x"},
\code{"y"} or \code{"xy"}.}

\item{xlab, ylab}{Axis labels.}

\item{show}{Names or indices of the variables to plot (default: all).}

\item{legend}{If \code{TRUE}, draw a legend.}

\item{draw}{Drawing function, typically \code{\link[graphics]{lines}} or
\code{\link[graphics]{points}}.}

\item{lwd}{Line width.}

\item{add}{If \code{TRUE}, add to the existing plot.}

\item{main, sub}{Plot title and subtitle.}

\item{colMap}{Optional mapping of columns to palette colour indices, used to
keep colours consistent between model and data.}

\item{pchMap}{Optional mapping of columns to plotting characters.}

\item{...}{Additional arguments passed on to \code{plot}.}
}
\value{
None; \code{timePlot} is called for the plot it draws.
}
\description{
\code{timePlot()} plots the columns of a data frame (time in the first
column, one or more variables in the rest) against time, colouring each
variable with the grindR palette. It is used internally by \code{\link{run}}
and \code{\link{fit}} to draw time courses, but can be called directly on any
such data frame, e.g. the result of \code{run(table = TRUE)}.
}
\examples{
model <- function(t, state, parms) {
  with(as.list(c(state, parms)), list(c(r * N * (1 - N / K))))
}
p <- c(r = 0.5, K = 10)
s <- c(N = 0.1)

out <- run(tmax = 20, table = TRUE, timeplot = FALSE)
timePlot(out)

}
\seealso{
\code{\link{run}}, \code{\link{fit}}
}
