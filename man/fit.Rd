% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grind.R
\name{fit}
\alias{fit}
\title{Estimate parameters by fitting a model to data}
\usage{
fit(
  datas = data,
  state = s,
  parms = p,
  odes = model,
  free = NULL,
  who = NULL,
  differ = NULL,
  fixed = NULL,
  tmin = 0,
  tmax = NULL,
  ymin = NULL,
  ymax = NULL,
  log = "",
  xlab = "Time",
  ylab = "Density",
  bootstrap = 0,
  show = NULL,
  fun = NULL,
  costfun = cost,
  logpar = FALSE,
  lower = -Inf,
  upper = Inf,
  initial = FALSE,
  add = FALSE,
  timeplot = TRUE,
  legend = TRUE,
  main = NULL,
  sub = NULL,
  pchMap = NULL,
  ...
)
}
\arguments{
\item{datas}{A data frame, or a list of data frames, of observations. The
first column is time; the remaining columns are observed variables, matched
by name to the state variables. Defaults to the global \code{data}.}

\item{state}{Named numeric vector of initial state values. Defaults to the
global \code{s}.}

\item{parms}{Named numeric vector of parameters. Defaults to the global
\code{p}.}

\item{odes}{The model function \code{f(t, state, parms)}. Defaults to the
global \code{model}.}

\item{free}{Names of parameters and/or initial state values to estimate
(shared across all datasets). Defaults to all when neither \code{free} nor
\code{differ} is given.}

\item{who}{Deprecated alias for \code{free}.}

\item{differ}{Names of parameters/states to estimate separately for each
dataset (one value per dataset); a character vector, or a named list of
starting values.}

\item{fixed}{Named list of per-dataset values that are held fixed (not
estimated); one value per dataset.}

\item{tmin, tmax}{Time range of the fit plot.}

\item{ymin, ymax}{Vertical range of the fit plot (\code{NULL} auto-scales).}

\item{log}{Which axes to draw on a log scale: \code{""}, \code{"x"},
\code{"y"} or \code{"xy"}.}

\item{xlab, ylab}{Axis labels of the fit plot.}

\item{bootstrap}{Number of bootstrap resamples for confidence intervals
(\code{0} = none).}

\item{show}{Names or indices of the variables to plot.}

\item{fun}{Optional function applied to both the data and the model output
before the cost is computed (e.g. a transform).}

\item{costfun}{Cost function to minimise; defaults to \code{\link{cost}}.}

\item{logpar}{If \code{TRUE}, estimate parameters on a log scale (keeping
them positive).}

\item{lower, upper}{Lower and upper bounds on the estimated parameters.}

\item{initial}{If \code{TRUE}, take each dataset's initial state from its
first row (which must be at time 0) rather than estimating it.}

\item{add}{If \code{TRUE}, add the fit plot to an existing one.}

\item{timeplot}{If \code{TRUE}, plot the fitted model against the data.}

\item{legend}{If \code{TRUE}, draw a legend.}

\item{main, sub}{Plot title and subtitle; may be one per dataset.}

\item{pchMap}{Optional mapping of data columns to plotting characters.}

\item{...}{Additional arguments passed on to \code{\link[FME]{modFit}},
\code{run} or \code{plot} (e.g. \code{method}).}
}
\value{
The \code{\link[FME]{modFit}} object (a list with the estimates in
  \code{$par}, the residual sum of squares in \code{$ssr}, and so on). If
  \code{bootstrap > 0}, a \code{$bootstrap} matrix of resampled estimates is
  added. The estimates and SSR are also printed.
}
\description{
\code{fit()} estimates free parameters and/or initial state values of an ODE
model by fitting it to one or more time-series datasets, minimising the sum
of squared residuals with \code{\link[FME]{modFit}} (via \code{\link{cost}}).
It can fit parameters shared across datasets (\code{free}), parameters that
take a separate value per dataset (\code{differ}), and per-dataset values
held fixed (\code{fixed}); it can fit on a log scale, draw the fit against
the data, and bootstrap confidence intervals.
}
\examples{
model <- function(t, state, parms) {
  with(as.list(c(state, parms)), list(c(r * N * (1 - N / K))))
}
p <- c(r = 0.5, K = 10)
s <- c(N = 0.1)

set.seed(1)
data <- run(tmax = 20, table = TRUE, timeplot = FALSE)   # synthetic data
data$N <- data$N + rnorm(nrow(data), sd = 0.2)
fit(data, free = c("r", "K"))

}
\seealso{
\code{\link{run}}, \code{\link{cost}}
}
